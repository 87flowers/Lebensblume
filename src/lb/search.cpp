#include "lb/search.h"

#include "lb/game.h"
#include "lb/types.h"
#include "lb/util/assert.h"

namespace lb::search {

  auto usiTime(Game &game, TimeSettings ts) -> void { lb_dbg("todo"); }
  auto usiDepth(Game &game, i64 depth) -> void { lb_dbg("todo"); }
  auto usiNode(Game &game, i64 nodes) -> void { lb_dbg("todo"); }

} // namespace lb::search
